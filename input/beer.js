var animBeer = lottie.loadAnimation({
    container: document.getElementById('anim-beer'), // récupérer l'ID de la div
    renderer: 'svg',
    loop: true,
    autoplay: true,
    path: 'beer.json' 
  });